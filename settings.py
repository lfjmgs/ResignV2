# coding=utf-8

# 相关配置(按实际修改)
keystore_file = 'xxx.jks'  # keystore文件路径
keystore_pass = '123456'  # keystore密码
channels_file = ''  # 渠道文件路径，不指定的话请在命令行第二个参数指定渠道号（可指定多个渠道，以逗号分隔）
sdk_dir = ''  # Android SDK路径，不指定则必须配置ANDROID_HOME环境变量
